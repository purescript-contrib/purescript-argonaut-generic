{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp --psc-package build -- --censor-lib --strict",
    "test": "pulp --psc-package test"
  },
  "devDependencies": {
    "pulp": "^12.2.0",
    "purescript-psa": "^0.5.0",
    "purescript": "^0.12.0",
    "rimraf": "^2.6.1"
  }
}